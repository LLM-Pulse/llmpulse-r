#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.46.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Prompts operations
#' @description PromptsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreatePrompts  ####################
#'
#' library(llmpulse)
#' var_prompts_create_request <- PromptsCreateRequest$new(123, c("prompts_example"), "country_code_example", "language_code_example") # PromptsCreateRequest | 
#'
#' #Bulk-create prompts
#' api_instance <- PromptsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreatePrompts(var_prompts_create_requestdata_file = "result.txt")
#' result <- api_instance$CreatePrompts(var_prompts_create_request)
#' dput(result)
#'
#'
#' ####################  DeletePrompt  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_id <- 56 # integer | 
#'
#' #Delete a prompt
#' api_instance <- PromptsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeletePrompt(var_project_id, var_id)
#'
#'
#' ####################  ListPromptExecutions  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- getTimeseries_collection_id_parameter$new() # GetTimeseriesCollectionIdParameter | One collection/tag ID or a comma-separated list of IDs (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_prompt <- 56 # integer | Filter by prompt ID (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_mention_filter <- "mention_filter_example" # character | Filter by which brands are mentioned, as a two-axis matrix (your brand x competitors): mentions_you / not_mentions_you, mentions_competitor / not_mentions_competitor, and the four combined cells you_and_competitor, competitor_not_you (a rival wins and you are absent), you_not_competitor, no_brands (no tracked brand appears, i.e. open space). Combine with 'competitors' to narrow the competitor side to specific rivals; on a negative cell that reads 'none of these'. On /dimensions/sources it applies to the crawled content of each cited page instead of the answer text. The legacy value 'competitors_only' is still accepted as an alias of competitor_not_you. (Optional)
#' var_citation_filter <- "citation_filter_example" # character | Same two-axis matrix applied to the domains cited in the answer instead of the brands named in it. Independent of mention_filter; pass both to intersect them (e.g. mentions_you + not_cites_you finds answers that talk about you without linking to you). (Optional)
#' var_competitors <- "competitors_example" # character | Comma-separated competitor IDs (unknown IDs return ERR_INVALID_PARAM) (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List prompt executions
#' api_instance <- PromptsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListPromptExecutions(var_project_id, page = var_page, per_page = var_per_page, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, prompt = var_prompt, from = var_from, to = var_to, mention_filter = var_mention_filter, citation_filter = var_citation_filter, competitors = var_competitors, output = var_output)
#'
#'
#' ####################  ListPrompts  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- getTimeseries_collection_id_parameter$new() # GetTimeseriesCollectionIdParameter | One collection/tag ID or a comma-separated list of IDs (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_prompt_type <- "prompt_type_example" # character | One prompt type or a comma-separated list: informational, navigational, commercial, transactional (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List prompts
#' api_instance <- PromptsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListPrompts(var_project_id, page = var_page, per_page = var_per_page, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, prompt_type = var_prompt_type, brand_kind = var_brand_kind, from = var_from, to = var_to, output = var_output)
#'
#'
#' ####################  ListQueryFanOuts  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_view <- "query" # character | Row shape: one per distinct sub-query, or one per prompt (Optional)
#' var_order <- "order_example" # character | Sort field; the allowed set depends on view (Optional)
#' var_direction <- "desc" # character |  (Optional)
#' var_query <- "query_example" # character | Case-insensitive substring filter on the sub-query text (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- getTimeseries_collection_id_parameter$new() # GetTimeseriesCollectionIdParameter | One collection/tag ID or a comma-separated list of IDs (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_prompt <- 56 # integer | Filter by prompt ID (Optional)
#' var_prompt_type <- "prompt_type_example" # character | One prompt type or a comma-separated list: informational, navigational, commercial, transactional (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List query fan-out
#' api_instance <- PromptsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListQueryFanOuts(var_project_id, page = var_page, per_page = var_per_page, view = var_view, order = var_order, direction = var_direction, query = var_query, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, prompt = var_prompt, prompt_type = var_prompt_type, brand_kind = var_brand_kind, range = var_range, from = var_from, to = var_to, output = var_output)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PromptsApi <- R6::R6Class(
  "PromptsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PromptsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Bulk-create prompts
    #'
    #' @param prompts_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PromptsCreateResponse
    CreatePrompts = function(prompts_create_request, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreatePromptsWithHttpInfo(prompts_create_request, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Bulk-create prompts
    #'
    #' @param prompts_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PromptsCreateResponse) with additional information such as HTTP status code, headers
    CreatePromptsWithHttpInfo = function(prompts_create_request, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`prompts_create_request`)) {
        stop("Missing required parameter `prompts_create_request`.")
      }

      if (!missing(`prompts_create_request`) && is.null(`prompts_create_request`)) {
        stop("Invalid value for `prompts_create_request` when calling PromptsApi$CreatePrompts, `prompts_create_request` is not nullable")
      }

      if (!is.null(`prompts_create_request`)) {
        local_var_body <- `prompts_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/prompts"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PromptsCreateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete a prompt
    #'
    #' @param project_id Project ID
    #' @param id 
    #' @param ... Other optional arguments
    #'
    #' @return void
    DeletePrompt = function(project_id, id, ...) {
      local_var_response <- self$DeletePromptWithHttpInfo(project_id, id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete a prompt
    #'
    #' @param project_id Project ID
    #' @param id 
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DeletePromptWithHttpInfo = function(project_id, id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling PromptsApi$DeletePrompt, `project_id` is not nullable")
      }

      if (!missing(`id`) && is.null(`id`)) {
        stop("Invalid value for `id` when calling PromptsApi$DeletePrompt, `id` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      local_var_url_path <- "/prompts/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List prompt executions
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param mention_filter (optional) Filter by which brands are mentioned, as a two-axis matrix (your brand x competitors): mentions_you / not_mentions_you, mentions_competitor / not_mentions_competitor, and the four combined cells you_and_competitor, competitor_not_you (a rival wins and you are absent), you_not_competitor, no_brands (no tracked brand appears, i.e. open space). Combine with 'competitors' to narrow the competitor side to specific rivals; on a negative cell that reads 'none of these'. On /dimensions/sources it applies to the crawled content of each cited page instead of the answer text. The legacy value 'competitors_only' is still accepted as an alias of competitor_not_you.
    #' @param citation_filter (optional) Same two-axis matrix applied to the domains cited in the answer instead of the brands named in it. Independent of mention_filter; pass both to intersect them (e.g. mentions_you + not_cites_you finds answers that talk about you without linking to you).
    #' @param competitors (optional) Comma-separated competitor IDs (unknown IDs return ERR_INVALID_PARAM)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListPromptExecutions = function(project_id, page = 1, per_page = 20, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, from = NULL, to = NULL, mention_filter = NULL, citation_filter = NULL, competitors = NULL, output = NULL, ...) {
      local_var_response <- self$ListPromptExecutionsWithHttpInfo(project_id, page, per_page, model, collection_id, country_code, language_code, prompt, from, to, mention_filter, citation_filter, competitors, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List prompt executions
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param mention_filter (optional) Filter by which brands are mentioned, as a two-axis matrix (your brand x competitors): mentions_you / not_mentions_you, mentions_competitor / not_mentions_competitor, and the four combined cells you_and_competitor, competitor_not_you (a rival wins and you are absent), you_not_competitor, no_brands (no tracked brand appears, i.e. open space). Combine with 'competitors' to narrow the competitor side to specific rivals; on a negative cell that reads 'none of these'. On /dimensions/sources it applies to the crawled content of each cited page instead of the answer text. The legacy value 'competitors_only' is still accepted as an alias of competitor_not_you.
    #' @param citation_filter (optional) Same two-axis matrix applied to the domains cited in the answer instead of the brands named in it. Independent of mention_filter; pass both to intersect them (e.g. mentions_you + not_cites_you finds answers that talk about you without linking to you).
    #' @param competitors (optional) Comma-separated competitor IDs (unknown IDs return ERR_INVALID_PARAM)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListPromptExecutionsWithHttpInfo = function(project_id, page = 1, per_page = 20, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, from = NULL, to = NULL, mention_filter = NULL, citation_filter = NULL, competitors = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling PromptsApi$ListPromptExecutions, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling PromptsApi$ListPromptExecutions, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling PromptsApi$ListPromptExecutions, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPromptExecutions, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPromptExecutions, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPromptExecutions, must be bigger than or equal to 1.")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling PromptsApi$ListPromptExecutions, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling PromptsApi$ListPromptExecutions, `collection_id` is not nullable")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling PromptsApi$ListPromptExecutions, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling PromptsApi$ListPromptExecutions, `language_code` is not nullable")
      }

      if (!missing(`prompt`) && is.null(`prompt`)) {
        stop("Invalid value for `prompt` when calling PromptsApi$ListPromptExecutions, `prompt` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling PromptsApi$ListPromptExecutions, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling PromptsApi$ListPromptExecutions, `to` is not nullable")
      }

      if (!missing(`mention_filter`) && is.null(`mention_filter`)) {
        stop("Invalid value for `mention_filter` when calling PromptsApi$ListPromptExecutions, `mention_filter` is not nullable")
      }

      if (!missing(`citation_filter`) && is.null(`citation_filter`)) {
        stop("Invalid value for `citation_filter` when calling PromptsApi$ListPromptExecutions, `citation_filter` is not nullable")
      }

      if (!missing(`competitors`) && is.null(`competitors`)) {
        stop("Invalid value for `competitors` when calling PromptsApi$ListPromptExecutions, `competitors` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling PromptsApi$ListPromptExecutions, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling PromptsApi$ListPromptExecutions. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["prompt"]] <- `prompt`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`mention_filter`) && !(`mention_filter` %in% c("mentions_you", "not_mentions_you", "mentions_competitor", "not_mentions_competitor", "you_and_competitor", "competitor_not_you", "you_not_competitor", "no_brands"))) {
        stop("Invalid value for mention_filter when calling PromptsApi$ListPromptExecutions. Must be [mentions_you, not_mentions_you, mentions_competitor, not_mentions_competitor, you_and_competitor, competitor_not_you, you_not_competitor, no_brands].")
      }
      query_params[["mention_filter"]] <- `mention_filter`

      if (!is.null(`citation_filter`) && !(`citation_filter` %in% c("cites_you", "not_cites_you", "cites_competitor", "not_cites_competitor", "you_and_competitor", "competitor_not_you", "you_not_competitor", "cites_no_brands"))) {
        stop("Invalid value for citation_filter when calling PromptsApi$ListPromptExecutions. Must be [cites_you, not_cites_you, cites_competitor, not_cites_competitor, you_and_competitor, competitor_not_you, you_not_competitor, cites_no_brands].")
      }
      query_params[["citation_filter"]] <- `citation_filter`

      query_params[["competitors"]] <- `competitors`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling PromptsApi$ListPromptExecutions. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/prompt_executions"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List prompts
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListPrompts = function(project_id, page = 1, per_page = 20, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt_type = NULL, brand_kind = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListPromptsWithHttpInfo(project_id, page, per_page, model, collection_id, country_code, language_code, prompt_type, brand_kind, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List prompts
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListPromptsWithHttpInfo = function(project_id, page = 1, per_page = 20, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt_type = NULL, brand_kind = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling PromptsApi$ListPrompts, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling PromptsApi$ListPrompts, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling PromptsApi$ListPrompts, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPrompts, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPrompts, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListPrompts, must be bigger than or equal to 1.")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling PromptsApi$ListPrompts, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling PromptsApi$ListPrompts, `collection_id` is not nullable")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling PromptsApi$ListPrompts, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling PromptsApi$ListPrompts, `language_code` is not nullable")
      }

      if (!missing(`prompt_type`) && is.null(`prompt_type`)) {
        stop("Invalid value for `prompt_type` when calling PromptsApi$ListPrompts, `prompt_type` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling PromptsApi$ListPrompts, `brand_kind` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling PromptsApi$ListPrompts, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling PromptsApi$ListPrompts, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling PromptsApi$ListPrompts, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling PromptsApi$ListPrompts. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["prompt_type"]] <- `prompt_type`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling PromptsApi$ListPrompts. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling PromptsApi$ListPrompts. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/prompts"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List query fan-out
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per distinct sub-query, or one per prompt (default value: "query")
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param query (optional) Case-insensitive substring filter on the sub-query text
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListQueryFanOuts = function(project_id, page = 1, per_page = 20, view = "query", order = NULL, direction = "desc", query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListQueryFanOutsWithHttpInfo(project_id, page, per_page, view, order, direction, query, model, collection_id, country_code, language_code, prompt, prompt_type, brand_kind, range, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List query fan-out
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per distinct sub-query, or one per prompt (default value: "query")
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param query (optional) Case-insensitive substring filter on the sub-query text
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListQueryFanOutsWithHttpInfo = function(project_id, page = 1, per_page = 20, view = "query", order = NULL, direction = "desc", query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling PromptsApi$ListQueryFanOuts, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling PromptsApi$ListQueryFanOuts, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling PromptsApi$ListQueryFanOuts, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListQueryFanOuts, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListQueryFanOuts, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling PromptsApi$ListQueryFanOuts, must be bigger than or equal to 1.")
      }

      if (!missing(`view`) && is.null(`view`)) {
        stop("Invalid value for `view` when calling PromptsApi$ListQueryFanOuts, `view` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling PromptsApi$ListQueryFanOuts, `order` is not nullable")
      }

      if (!missing(`direction`) && is.null(`direction`)) {
        stop("Invalid value for `direction` when calling PromptsApi$ListQueryFanOuts, `direction` is not nullable")
      }

      if (!missing(`query`) && is.null(`query`)) {
        stop("Invalid value for `query` when calling PromptsApi$ListQueryFanOuts, `query` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling PromptsApi$ListQueryFanOuts, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling PromptsApi$ListQueryFanOuts, `collection_id` is not nullable")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling PromptsApi$ListQueryFanOuts, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling PromptsApi$ListQueryFanOuts, `language_code` is not nullable")
      }

      if (!missing(`prompt`) && is.null(`prompt`)) {
        stop("Invalid value for `prompt` when calling PromptsApi$ListQueryFanOuts, `prompt` is not nullable")
      }

      if (!missing(`prompt_type`) && is.null(`prompt_type`)) {
        stop("Invalid value for `prompt_type` when calling PromptsApi$ListQueryFanOuts, `prompt_type` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling PromptsApi$ListQueryFanOuts, `brand_kind` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling PromptsApi$ListQueryFanOuts, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling PromptsApi$ListQueryFanOuts, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling PromptsApi$ListQueryFanOuts, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling PromptsApi$ListQueryFanOuts, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`view`) && !(`view` %in% c("query", "prompt"))) {
        stop("Invalid value for view when calling PromptsApi$ListQueryFanOuts. Must be [query, prompt].")
      }
      query_params[["view"]] <- `view`

      if (!is.null(`order`) && !(`order` %in% c("count", "share", "query_text", "total_count", "variations_count", "prompt_text"))) {
        stop("Invalid value for order when calling PromptsApi$ListQueryFanOuts. Must be [count, share, query_text, total_count, variations_count, prompt_text].")
      }
      query_params[["order"]] <- `order`

      if (!is.null(`direction`) && !(`direction` %in% c("asc", "desc"))) {
        stop("Invalid value for direction when calling PromptsApi$ListQueryFanOuts. Must be [asc, desc].")
      }
      query_params[["direction"]] <- `direction`

      query_params[["query"]] <- `query`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling PromptsApi$ListQueryFanOuts. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["prompt"]] <- `prompt`

      query_params[["prompt_type"]] <- `prompt_type`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling PromptsApi$ListQueryFanOuts. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling PromptsApi$ListQueryFanOuts. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/query_fan_outs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
