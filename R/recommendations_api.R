#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.22.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Recommendations operations
#' @description RecommendationsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetRecommendation  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_id <- 56 # integer | 
#' var_item_status <- "item_status_example" # character |  (Optional)
#' var_resolve_source_refs <- TRUE # character |  (Optional)
#'
#' #Get recommendation run with items
#' api_instance <- RecommendationsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetRecommendation(var_project_id, var_id, item_status = var_item_status, resolve_source_refs = var_resolve_source_refs)
#'
#'
#' ####################  LaunchRecommendations  ####################
#'
#' library(llmpulse)
#' var_launch_recommendations_request <- launchRecommendations_request$new(123, "ai_visibility") # LaunchRecommendationsRequest | 
#'
#' #Launch a recommendations generation
#' api_instance <- RecommendationsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$LaunchRecommendations(var_launch_recommendations_request)
#'
#'
#' ####################  ListRecommendations  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_recommendation_type <- "recommendation_type_example" # character |  (Optional)
#' var_status <- "status_example" # character |  (Optional)
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#'
#' #List recommendation runs
#' api_instance <- RecommendationsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListRecommendations(var_project_id, recommendation_type = var_recommendation_type, status = var_status, page = var_page, per_page = var_per_page)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RecommendationsApi <- R6::R6Class(
  "RecommendationsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new RecommendationsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get recommendation run with items
    #'
    #' @param project_id Project ID
    #' @param id 
    #' @param item_status (optional) No description
    #' @param resolve_source_refs (optional) No description (default value: TRUE)
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetRecommendation = function(project_id, id, item_status = NULL, resolve_source_refs = TRUE, ...) {
      local_var_response <- self$GetRecommendationWithHttpInfo(project_id, id, item_status, resolve_source_refs, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get recommendation run with items
    #'
    #' @param project_id Project ID
    #' @param id 
    #' @param item_status (optional) No description
    #' @param resolve_source_refs (optional) No description (default value: TRUE)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetRecommendationWithHttpInfo = function(project_id, id, item_status = NULL, resolve_source_refs = TRUE, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling RecommendationsApi$GetRecommendation, `project_id` is not nullable")
      }

      if (!missing(`id`) && is.null(`id`)) {
        stop("Invalid value for `id` when calling RecommendationsApi$GetRecommendation, `id` is not nullable")
      }

      if (!missing(`item_status`) && is.null(`item_status`)) {
        stop("Invalid value for `item_status` when calling RecommendationsApi$GetRecommendation, `item_status` is not nullable")
      }

      if (!missing(`resolve_source_refs`) && is.null(`resolve_source_refs`)) {
        stop("Invalid value for `resolve_source_refs` when calling RecommendationsApi$GetRecommendation, `resolve_source_refs` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      if (!is.null(`item_status`) && !(`item_status` %in% c("active", "completed", "archived"))) {
        stop("Invalid value for item_status when calling RecommendationsApi$GetRecommendation. Must be [active, completed, archived].")
      }
      query_params[["item_status"]] <- `item_status`

      query_params[["resolve_source_refs"]] <- `resolve_source_refs`

      local_var_url_path <- "/recommendations/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Launch a recommendations generation
    #'
    #' @param launch_recommendations_request 
    #' @param ... Other optional arguments
    #'
    #' @return void
    LaunchRecommendations = function(launch_recommendations_request, ...) {
      local_var_response <- self$LaunchRecommendationsWithHttpInfo(launch_recommendations_request, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Launch a recommendations generation
    #'
    #' @param launch_recommendations_request 
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LaunchRecommendationsWithHttpInfo = function(launch_recommendations_request, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`launch_recommendations_request`)) {
        stop("Missing required parameter `launch_recommendations_request`.")
      }

      if (!missing(`launch_recommendations_request`) && is.null(`launch_recommendations_request`)) {
        stop("Invalid value for `launch_recommendations_request` when calling RecommendationsApi$LaunchRecommendations, `launch_recommendations_request` is not nullable")
      }

      if (!is.null(`launch_recommendations_request`)) {
        local_var_body <- `launch_recommendations_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/recommendations"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List recommendation runs
    #'
    #' @param project_id Project ID
    #' @param recommendation_type (optional) No description
    #' @param status (optional) No description
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListRecommendations = function(project_id, recommendation_type = NULL, status = NULL, page = 1, per_page = 20, ...) {
      local_var_response <- self$ListRecommendationsWithHttpInfo(project_id, recommendation_type, status, page, per_page, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List recommendation runs
    #'
    #' @param project_id Project ID
    #' @param recommendation_type (optional) No description
    #' @param status (optional) No description
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListRecommendationsWithHttpInfo = function(project_id, recommendation_type = NULL, status = NULL, page = 1, per_page = 20, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling RecommendationsApi$ListRecommendations, `project_id` is not nullable")
      }

      if (!missing(`recommendation_type`) && is.null(`recommendation_type`)) {
        stop("Invalid value for `recommendation_type` when calling RecommendationsApi$ListRecommendations, `recommendation_type` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling RecommendationsApi$ListRecommendations, `status` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling RecommendationsApi$ListRecommendations, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling RecommendationsApi$ListRecommendations, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling RecommendationsApi$ListRecommendations, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling RecommendationsApi$ListRecommendations, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling RecommendationsApi$ListRecommendations, must be bigger than or equal to 1.")
      }

      query_params[["project_id"]] <- `project_id`

      if (!is.null(`recommendation_type`) && !(`recommendation_type` %in% c("ai_visibility", "social_community", "brand_building", "sentiment_reputation"))) {
        stop("Invalid value for recommendation_type when calling RecommendationsApi$ListRecommendations. Must be [ai_visibility, social_community, brand_building, sentiment_reputation].")
      }
      query_params[["recommendation_type"]] <- `recommendation_type`

      if (!is.null(`status`) && !(`status` %in% c("pending", "processing", "completed", "failed"))) {
        stop("Invalid value for status when calling RecommendationsApi$ListRecommendations. Must be [pending, processing, completed, failed].")
      }
      query_params[["status"]] <- `status`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      local_var_url_path <- "/recommendations"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
