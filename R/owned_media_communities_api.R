#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.46.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OwnedMediaCommunities operations
#' @description OwnedMediaCommunitiesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ListOwnedMedia  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_provider <- "provider_example" # character | The platform to report on
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_view <- "view_example" # character | Row shape; the allowed set depends on provider (Optional)
#' var_store <- "google_play" # character | provider=mobile_apps only (Optional)
#' var_owned <- "owned_example" # character | Return only rows belonging to the account's own connected profile (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- "12,34" # character | One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize. (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List owned-media citations
#' api_instance <- OwnedMediaCommunitiesApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListOwnedMedia(var_project_id, var_provider, page = var_page, per_page = var_per_page, view = var_view, store = var_store, owned = var_owned, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, brand_kind = var_brand_kind, range = var_range, from = var_from, to = var_to, output = var_output)
#'
#'
#' ####################  ListRedditCitations  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_view <- "subreddits" # character |  (Optional)
#' var_subreddit <- "subreddit_example" # character | Filter to one subreddit (name without the r/ prefix) (Optional)
#' var_author <- "author_example" # character | Filter to one Reddit author (Optional)
#' var_status <- "status_example" # character | view=threads only (Optional)
#' var_owned <- "owned_example" # character | Return only subreddits/authors the account has claimed as its own (Optional)
#' var_brand <- "brand_example" # character | Filter to citations whose scraped Reddit content mentions a brand: 'brand' for the tracked brand, or a competitor id. Reads the page content, not the AI answer. (Optional)
#' var_order <- "order_example" # character | Sort field; the allowed set depends on view (Optional)
#' var_direction <- "desc" # character |  (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- "12,34" # character | One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize. (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List cited Reddit content
#' api_instance <- OwnedMediaCommunitiesApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListRedditCitations(var_project_id, page = var_page, per_page = var_per_page, view = var_view, subreddit = var_subreddit, author = var_author, status = var_status, owned = var_owned, brand = var_brand, order = var_order, direction = var_direction, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, brand_kind = var_brand_kind, range = var_range, from = var_from, to = var_to, output = var_output)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OwnedMediaCommunitiesApi <- R6::R6Class(
  "OwnedMediaCommunitiesApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new OwnedMediaCommunitiesApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' List owned-media citations
    #'
    #' @param project_id Project ID
    #' @param provider The platform to report on
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape; the allowed set depends on provider
    #' @param store (optional) provider=mobile_apps only (default value: "google_play")
    #' @param owned (optional) Return only rows belonging to the account's own connected profile
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListOwnedMedia = function(project_id, provider, page = 1, per_page = 20, view = NULL, store = "google_play", owned = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListOwnedMediaWithHttpInfo(project_id, provider, page, per_page, view, store, owned, model, collection_id, country_code, language_code, brand_kind, range, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List owned-media citations
    #'
    #' @param project_id Project ID
    #' @param provider The platform to report on
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape; the allowed set depends on provider
    #' @param store (optional) provider=mobile_apps only (default value: "google_play")
    #' @param owned (optional) Return only rows belonging to the account's own connected profile
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListOwnedMediaWithHttpInfo = function(project_id, provider, page = 1, per_page = 20, view = NULL, store = "google_play", owned = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`provider`)) {
        stop("Missing required parameter `provider`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `project_id` is not nullable")
      }

      if (!missing(`provider`) && is.null(`provider`)) {
        stop("Invalid value for `provider` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `provider` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, must be bigger than or equal to 1.")
      }

      if (!missing(`view`) && is.null(`view`)) {
        stop("Invalid value for `view` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `view` is not nullable")
      }

      if (!missing(`store`) && is.null(`store`)) {
        stop("Invalid value for `store` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `store` is not nullable")
      }

      if (!missing(`owned`) && is.null(`owned`)) {
        stop("Invalid value for `owned` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `owned` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `collection_id` is not nullable")
      }
      if (!is.null(`collection_id`) && !stringr::str_detect(`collection_id`, "^\\d+(,\\d+)*$")) {
        stop("Invalid value for `collection_id` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, must conform to the pattern ^\\d+(,\\d+)*$.")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `language_code` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `brand_kind` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling OwnedMediaCommunitiesApi$ListOwnedMedia, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`provider`) && !(`provider` %in% c("youtube", "instagram", "facebook", "tiktok", "linkedin", "mobile_apps"))) {
        stop("Invalid value for provider when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [youtube, instagram, facebook, tiktok, linkedin, mobile_apps].")
      }
      query_params[["provider"]] <- `provider`

      if (!is.null(`view`) && !(`view` %in% c("videos", "channels", "posts", "profiles", "own_citations", "apps"))) {
        stop("Invalid value for view when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [videos, channels, posts, profiles, own_citations, apps].")
      }
      query_params[["view"]] <- `view`

      if (!is.null(`store`) && !(`store` %in% c("google_play", "app_store"))) {
        stop("Invalid value for store when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [google_play, app_store].")
      }
      query_params[["store"]] <- `store`

      query_params[["owned"]] <- `owned`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling OwnedMediaCommunitiesApi$ListOwnedMedia. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/owned_media"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List cited Reddit content
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) No description (default value: "subreddits")
    #' @param subreddit (optional) Filter to one subreddit (name without the r/ prefix)
    #' @param author (optional) Filter to one Reddit author
    #' @param status (optional) view=threads only
    #' @param owned (optional) Return only subreddits/authors the account has claimed as its own
    #' @param brand (optional) Filter to citations whose scraped Reddit content mentions a brand: 'brand' for the tracked brand, or a competitor id. Reads the page content, not the AI answer.
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListRedditCitations = function(project_id, page = 1, per_page = 20, view = "subreddits", subreddit = NULL, author = NULL, status = NULL, owned = NULL, brand = NULL, order = NULL, direction = "desc", model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListRedditCitationsWithHttpInfo(project_id, page, per_page, view, subreddit, author, status, owned, brand, order, direction, model, collection_id, country_code, language_code, brand_kind, range, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List cited Reddit content
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) No description (default value: "subreddits")
    #' @param subreddit (optional) Filter to one subreddit (name without the r/ prefix)
    #' @param author (optional) Filter to one Reddit author
    #' @param status (optional) view=threads only
    #' @param owned (optional) Return only subreddits/authors the account has claimed as its own
    #' @param brand (optional) Filter to citations whose scraped Reddit content mentions a brand: 'brand' for the tracked brand, or a competitor id. Reads the page content, not the AI answer.
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListRedditCitationsWithHttpInfo = function(project_id, page = 1, per_page = 20, view = "subreddits", subreddit = NULL, author = NULL, status = NULL, owned = NULL, brand = NULL, order = NULL, direction = "desc", model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling OwnedMediaCommunitiesApi$ListRedditCitations, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListRedditCitations, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling OwnedMediaCommunitiesApi$ListRedditCitations, must be bigger than or equal to 1.")
      }

      if (!missing(`view`) && is.null(`view`)) {
        stop("Invalid value for `view` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `view` is not nullable")
      }

      if (!missing(`subreddit`) && is.null(`subreddit`)) {
        stop("Invalid value for `subreddit` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `subreddit` is not nullable")
      }

      if (!missing(`author`) && is.null(`author`)) {
        stop("Invalid value for `author` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `author` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `status` is not nullable")
      }

      if (!missing(`owned`) && is.null(`owned`)) {
        stop("Invalid value for `owned` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `owned` is not nullable")
      }

      if (!missing(`brand`) && is.null(`brand`)) {
        stop("Invalid value for `brand` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `brand` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `order` is not nullable")
      }

      if (!missing(`direction`) && is.null(`direction`)) {
        stop("Invalid value for `direction` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `direction` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `collection_id` is not nullable")
      }
      if (!is.null(`collection_id`) && !stringr::str_detect(`collection_id`, "^\\d+(,\\d+)*$")) {
        stop("Invalid value for `collection_id` when calling OwnedMediaCommunitiesApi$ListRedditCitations, must conform to the pattern ^\\d+(,\\d+)*$.")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `language_code` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `brand_kind` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling OwnedMediaCommunitiesApi$ListRedditCitations, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`view`) && !(`view` %in% c("subreddits", "authors", "threads"))) {
        stop("Invalid value for view when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [subreddits, authors, threads].")
      }
      query_params[["view"]] <- `view`

      query_params[["subreddit"]] <- `subreddit`

      query_params[["author"]] <- `author`

      if (!is.null(`status`) && !(`status` %in% c("open", "archived"))) {
        stop("Invalid value for status when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [open, archived].")
      }
      query_params[["status"]] <- `status`

      query_params[["owned"]] <- `owned`

      query_params[["brand"]] <- `brand`

      if (!is.null(`order`) && !(`order` %in% c("citations", "subreddit", "unique_authors", "positive_pct", "negative_pct", "author", "avg_position", "upvotes", "comments", "sentiment"))) {
        stop("Invalid value for order when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [citations, subreddit, unique_authors, positive_pct, negative_pct, author, avg_position, upvotes, comments, sentiment].")
      }
      query_params[["order"]] <- `order`

      if (!is.null(`direction`) && !(`direction` %in% c("asc", "desc"))) {
        stop("Invalid value for direction when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [asc, desc].")
      }
      query_params[["direction"]] <- `direction`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling OwnedMediaCommunitiesApi$ListRedditCitations. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/reddit"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
