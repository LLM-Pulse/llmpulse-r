#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.48.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AIAgentTraffic operations
#' @description AIAgentTrafficApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetAgentTraffic  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_bot <- "bot_example" # character | Filter by bot slug (e.g. gptbot, claudebot, perplexitybot) (Optional)
#' var_company <- "company_example" # character | Filter by company (e.g. openai, anthropic, google) (Optional)
#' var_group_by <- "bot" # character |  (Optional)
#' var_granularity <- "granularity_example" # character |  (Optional)
#'
#' #AI bot crawler traffic (Scale plan or above, Beta)
#' api_instance <- AIAgentTrafficApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAgentTraffic(var_project_id, range = var_range, from = var_from, to = var_to, bot = var_bot, company = var_company, group_by = var_group_by, granularity = var_granularitydata_file = "result.txt")
#' result <- api_instance$GetAgentTraffic(var_project_id, range = var_range, from = var_from, to = var_to, bot = var_bot, company = var_company, group_by = var_group_by, granularity = var_granularity)
#' dput(result)
#'
#'
#' ####################  GetAiTraffic  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_source <- "source_example" # character | Filter by a single AI source slug (e.g. chatgpt, perplexity, gemini, claude) (Optional)
#' var_granularity <- "granularity_example" # character |  (Optional)
#'
#' #AI referral traffic (Scale plan or above)
#' api_instance <- AIAgentTrafficApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetAiTraffic(var_project_id, range = var_range, from = var_from, to = var_to, source = var_source, granularity = var_granularity)
#'
#'
#' ####################  ListAgentBots  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #AI bot catalog (Scale plan or above)
#' api_instance <- AIAgentTrafficApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAgentBots(var_project_id, output = var_outputdata_file = "result.txt")
#' result <- api_instance$ListAgentBots(var_project_id, output = var_output)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AIAgentTrafficApi <- R6::R6Class(
  "AIAgentTrafficApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AIAgentTrafficApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' AI bot crawler traffic (Scale plan or above, Beta)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param bot (optional) Filter by bot slug (e.g. gptbot, claudebot, perplexitybot)
    #' @param company (optional) Filter by company (e.g. openai, anthropic, google)
    #' @param group_by (optional) No description (default value: "bot")
    #' @param granularity (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AgentTrafficResponse
    GetAgentTraffic = function(project_id, range = NULL, from = NULL, to = NULL, bot = NULL, company = NULL, group_by = "bot", granularity = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAgentTrafficWithHttpInfo(project_id, range, from, to, bot, company, group_by, granularity, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' AI bot crawler traffic (Scale plan or above, Beta)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param bot (optional) Filter by bot slug (e.g. gptbot, claudebot, perplexitybot)
    #' @param company (optional) Filter by company (e.g. openai, anthropic, google)
    #' @param group_by (optional) No description (default value: "bot")
    #' @param granularity (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AgentTrafficResponse) with additional information such as HTTP status code, headers
    GetAgentTrafficWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, bot = NULL, company = NULL, group_by = "bot", granularity = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling AIAgentTrafficApi$GetAgentTraffic, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling AIAgentTrafficApi$GetAgentTraffic, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling AIAgentTrafficApi$GetAgentTraffic, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling AIAgentTrafficApi$GetAgentTraffic, `to` is not nullable")
      }

      if (!missing(`bot`) && is.null(`bot`)) {
        stop("Invalid value for `bot` when calling AIAgentTrafficApi$GetAgentTraffic, `bot` is not nullable")
      }

      if (!missing(`company`) && is.null(`company`)) {
        stop("Invalid value for `company` when calling AIAgentTrafficApi$GetAgentTraffic, `company` is not nullable")
      }

      if (!missing(`group_by`) && is.null(`group_by`)) {
        stop("Invalid value for `group_by` when calling AIAgentTrafficApi$GetAgentTraffic, `group_by` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling AIAgentTrafficApi$GetAgentTraffic, `granularity` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      query_params[["bot"]] <- `bot`

      query_params[["company"]] <- `company`

      if (!is.null(`group_by`) && !(`group_by` %in% c("bot", "company"))) {
        stop("Invalid value for group_by when calling AIAgentTrafficApi$GetAgentTraffic. Must be [bot, company].")
      }
      query_params[["group_by"]] <- `group_by`

      if (!is.null(`granularity`) && !(`granularity` %in% c("day", "week", "month"))) {
        stop("Invalid value for granularity when calling AIAgentTrafficApi$GetAgentTraffic. Must be [day, week, month].")
      }
      query_params[["granularity"]] <- `granularity`

      local_var_url_path <- "/metrics/agent_traffic"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AgentTrafficResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' AI referral traffic (Scale plan or above)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param source (optional) Filter by a single AI source slug (e.g. chatgpt, perplexity, gemini, claude)
    #' @param granularity (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetAiTraffic = function(project_id, range = NULL, from = NULL, to = NULL, source = NULL, granularity = NULL, ...) {
      local_var_response <- self$GetAiTrafficWithHttpInfo(project_id, range, from, to, source, granularity, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' AI referral traffic (Scale plan or above)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param source (optional) Filter by a single AI source slug (e.g. chatgpt, perplexity, gemini, claude)
    #' @param granularity (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetAiTrafficWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, source = NULL, granularity = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling AIAgentTrafficApi$GetAiTraffic, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling AIAgentTrafficApi$GetAiTraffic, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling AIAgentTrafficApi$GetAiTraffic, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling AIAgentTrafficApi$GetAiTraffic, `to` is not nullable")
      }

      if (!missing(`source`) && is.null(`source`)) {
        stop("Invalid value for `source` when calling AIAgentTrafficApi$GetAiTraffic, `source` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling AIAgentTrafficApi$GetAiTraffic, `granularity` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      query_params[["source"]] <- `source`

      if (!is.null(`granularity`) && !(`granularity` %in% c("day", "week", "month"))) {
        stop("Invalid value for granularity when calling AIAgentTrafficApi$GetAiTraffic. Must be [day, week, month].")
      }
      query_params[["granularity"]] <- `granularity`

      local_var_url_path <- "/metrics/ai_traffic"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' AI bot catalog (Scale plan or above)
    #'
    #' @param project_id Project ID
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AgentBotsResponse
    ListAgentBots = function(project_id, output = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListAgentBotsWithHttpInfo(project_id, output, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' AI bot catalog (Scale plan or above)
    #'
    #' @param project_id Project ID
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AgentBotsResponse) with additional information such as HTTP status code, headers
    ListAgentBotsWithHttpInfo = function(project_id, output = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling AIAgentTrafficApi$ListAgentBots, `project_id` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling AIAgentTrafficApi$ListAgentBots, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling AIAgentTrafficApi$ListAgentBots. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/agent_bots"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AgentBotsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
