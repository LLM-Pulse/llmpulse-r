#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.46.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title TechnicalGEOReports operations
#' @description TechnicalGEOReportsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateTechnicalGeoReports  ####################
#'
#' library(llmpulse)
#' var_create_technical_geo_reports_request <- createTechnicalGeoReports_request$new(123, "url_example", "country_code_example") # CreateTechnicalGeoReportsRequest | 
#'
#' #Run technical GEO analysis
#' api_instance <- TechnicalGEOReportsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$CreateTechnicalGeoReports(var_create_technical_geo_reports_request)
#'
#'
#' ####################  GetTechnicalGeoReport  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_report_type <- "report_type_example" # character | 
#' var_id <- 56 # integer | Report id returned by POST /technical_geo_reports or GET /technical_geo_reports
#'
#' #Get a technical GEO report
#' api_instance <- TechnicalGEOReportsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetTechnicalGeoReport(var_project_id, var_report_type, var_id)
#'
#'
#' ####################  ListTechnicalGeoReports  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_report_type <- "report_type_example" # character | 
#' var_status <- "status_example" # character | Optional status filter; valid values depend on report_type (Optional)
#' var_batch_id <- 56 # integer | Optional batch id returned when the report bundle was created (Optional)
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#'
#' #List technical GEO reports
#' api_instance <- TechnicalGEOReportsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListTechnicalGeoReports(var_project_id, var_report_type, status = var_status, batch_id = var_batch_id, page = var_page, per_page = var_per_page)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
TechnicalGEOReportsApi <- R6::R6Class(
  "TechnicalGEOReportsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new TechnicalGEOReportsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Run technical GEO analysis
    #'
    #' @param create_technical_geo_reports_request 
    #' @param ... Other optional arguments
    #'
    #' @return void
    CreateTechnicalGeoReports = function(create_technical_geo_reports_request, ...) {
      local_var_response <- self$CreateTechnicalGeoReportsWithHttpInfo(create_technical_geo_reports_request, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Run technical GEO analysis
    #'
    #' @param create_technical_geo_reports_request 
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CreateTechnicalGeoReportsWithHttpInfo = function(create_technical_geo_reports_request, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`create_technical_geo_reports_request`)) {
        stop("Missing required parameter `create_technical_geo_reports_request`.")
      }

      if (!missing(`create_technical_geo_reports_request`) && is.null(`create_technical_geo_reports_request`)) {
        stop("Invalid value for `create_technical_geo_reports_request` when calling TechnicalGEOReportsApi$CreateTechnicalGeoReports, `create_technical_geo_reports_request` is not nullable")
      }

      if (!is.null(`create_technical_geo_reports_request`)) {
        local_var_body <- `create_technical_geo_reports_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/technical_geo_reports"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get a technical GEO report
    #'
    #' @param project_id Project ID
    #' @param report_type 
    #' @param id Report id returned by POST /technical_geo_reports or GET /technical_geo_reports
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetTechnicalGeoReport = function(project_id, report_type, id, ...) {
      local_var_response <- self$GetTechnicalGeoReportWithHttpInfo(project_id, report_type, id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get a technical GEO report
    #'
    #' @param project_id Project ID
    #' @param report_type 
    #' @param id Report id returned by POST /technical_geo_reports or GET /technical_geo_reports
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetTechnicalGeoReportWithHttpInfo = function(project_id, report_type, id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`report_type`)) {
        stop("Missing required parameter `report_type`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling TechnicalGEOReportsApi$GetTechnicalGeoReport, `project_id` is not nullable")
      }

      if (!missing(`report_type`) && is.null(`report_type`)) {
        stop("Invalid value for `report_type` when calling TechnicalGEOReportsApi$GetTechnicalGeoReport, `report_type` is not nullable")
      }

      if (!missing(`id`) && is.null(`id`)) {
        stop("Invalid value for `id` when calling TechnicalGEOReportsApi$GetTechnicalGeoReport, `id` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      if (!is.null(`report_type`) && !(`report_type` %in% c("crawlability", "schema", "content_readiness", "discoverability", "site_structure", "robots_txt", "agent_readiness", "llms_txt", "ai_visibility"))) {
        stop("Invalid value for report_type when calling TechnicalGEOReportsApi$GetTechnicalGeoReport. Must be [crawlability, schema, content_readiness, discoverability, site_structure, robots_txt, agent_readiness, llms_txt, ai_visibility].")
      }
      query_params[["report_type"]] <- `report_type`

      local_var_url_path <- "/technical_geo_reports/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List technical GEO reports
    #'
    #' @param project_id Project ID
    #' @param report_type 
    #' @param status (optional) Optional status filter; valid values depend on report_type
    #' @param batch_id (optional) Optional batch id returned when the report bundle was created
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListTechnicalGeoReports = function(project_id, report_type, status = NULL, batch_id = NULL, page = 1, per_page = 20, ...) {
      local_var_response <- self$ListTechnicalGeoReportsWithHttpInfo(project_id, report_type, status, batch_id, page, per_page, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List technical GEO reports
    #'
    #' @param project_id Project ID
    #' @param report_type 
    #' @param status (optional) Optional status filter; valid values depend on report_type
    #' @param batch_id (optional) Optional batch id returned when the report bundle was created
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListTechnicalGeoReportsWithHttpInfo = function(project_id, report_type, status = NULL, batch_id = NULL, page = 1, per_page = 20, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`report_type`)) {
        stop("Missing required parameter `report_type`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `project_id` is not nullable")
      }

      if (!missing(`report_type`) && is.null(`report_type`)) {
        stop("Invalid value for `report_type` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `report_type` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `status` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `batch_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling TechnicalGEOReportsApi$ListTechnicalGeoReports, must be bigger than or equal to 1.")
      }

      query_params[["project_id"]] <- `project_id`

      if (!is.null(`report_type`) && !(`report_type` %in% c("crawlability", "schema", "content_readiness", "discoverability", "site_structure", "robots_txt", "agent_readiness", "llms_txt", "ai_visibility"))) {
        stop("Invalid value for report_type when calling TechnicalGEOReportsApi$ListTechnicalGeoReports. Must be [crawlability, schema, content_readiness, discoverability, site_structure, robots_txt, agent_readiness, llms_txt, ai_visibility].")
      }
      query_params[["report_type"]] <- `report_type`

      query_params[["status"]] <- `status`

      query_params[["batch_id"]] <- `batch_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      local_var_url_path <- "/technical_geo_reports"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
