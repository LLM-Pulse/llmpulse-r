#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.46.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ShoppingAds operations
#' @description ShoppingAdsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ListAds  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_view <- "advertisers" # character | Row shape: one per advertising domain, or one per placement (Optional)
#' var_owned <- "owned_example" # character | Return only placements identified as the tracked brand's own (view=ads) (Optional)
#' var_order <- "order_example" # character | Sort field; the allowed set depends on view (Optional)
#' var_direction <- "direction_example" # character | Sort direction for view=advertisers. Defaults to desc, except avg_position and domain which default to asc. (Optional)
#' var_query <- "query_example" # character | Case-insensitive substring filter on the ad title, domain or snippet (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- "12,34" # character | One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize. (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_prompt <- 56 # integer | Filter by prompt ID (Optional)
#' var_prompt_type <- "prompt_type_example" # character | One prompt type or a comma-separated list: informational, navigational, commercial, transactional (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List AI ad placements
#' api_instance <- ShoppingAdsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListAds(var_project_id, page = var_page, per_page = var_per_page, view = var_view, owned = var_owned, order = var_order, direction = var_direction, query = var_query, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, prompt = var_prompt, prompt_type = var_prompt_type, brand_kind = var_brand_kind, range = var_range, from = var_from, to = var_to, output = var_output)
#'
#'
#' ####################  ListShopping  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_view <- "products" # character | Row shape: one per distinct product, or one per merchant (Optional)
#' var_owned <- "owned_example" # character | Return only products identified as the tracked brand's own. On view=merchants this narrows to the merchants selling those products; the totals block stays account-wide. (Optional)
#' var_order <- "order_example" # character | Sort field; the allowed set depends on view (Optional)
#' var_direction <- "desc" # character |  (Optional)
#' var_query <- "query_example" # character | Case-insensitive substring filter on the product title (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- "12,34" # character | One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize. (Optional)
#' var_country_code <- "country_code_example" # character | One ISO country code or a comma-separated list (e.g. US,GB,DE) (Optional)
#' var_language_code <- "language_code_example" # character | One ISO language code or a comma-separated list (e.g. en,es,de) (Optional)
#' var_prompt <- 56 # integer | Filter by prompt ID (Optional)
#' var_prompt_type <- "prompt_type_example" # character | One prompt type or a comma-separated list: informational, navigational, commercial, transactional (Optional)
#' var_brand_kind <- "brand_kind_example" # character | Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default. (Optional)
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#'
#' #List shopping results
#' api_instance <- ShoppingAdsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListShopping(var_project_id, page = var_page, per_page = var_per_page, view = var_view, owned = var_owned, order = var_order, direction = var_direction, query = var_query, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, prompt = var_prompt, prompt_type = var_prompt_type, brand_kind = var_brand_kind, range = var_range, from = var_from, to = var_to, output = var_output)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ShoppingAdsApi <- R6::R6Class(
  "ShoppingAdsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ShoppingAdsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' List AI ad placements
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per advertising domain, or one per placement (default value: "advertisers")
    #' @param owned (optional) Return only placements identified as the tracked brand's own (view=ads)
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) Sort direction for view=advertisers. Defaults to desc, except avg_position and domain which default to asc.
    #' @param query (optional) Case-insensitive substring filter on the ad title, domain or snippet
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListAds = function(project_id, page = 1, per_page = 20, view = "advertisers", owned = NULL, order = NULL, direction = NULL, query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListAdsWithHttpInfo(project_id, page, per_page, view, owned, order, direction, query, model, collection_id, country_code, language_code, prompt, prompt_type, brand_kind, range, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List AI ad placements
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per advertising domain, or one per placement (default value: "advertisers")
    #' @param owned (optional) Return only placements identified as the tracked brand's own (view=ads)
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) Sort direction for view=advertisers. Defaults to desc, except avg_position and domain which default to asc.
    #' @param query (optional) Case-insensitive substring filter on the ad title, domain or snippet
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListAdsWithHttpInfo = function(project_id, page = 1, per_page = 20, view = "advertisers", owned = NULL, order = NULL, direction = NULL, query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ShoppingAdsApi$ListAds, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling ShoppingAdsApi$ListAds, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling ShoppingAdsApi$ListAds, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListAds, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListAds, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListAds, must be bigger than or equal to 1.")
      }

      if (!missing(`view`) && is.null(`view`)) {
        stop("Invalid value for `view` when calling ShoppingAdsApi$ListAds, `view` is not nullable")
      }

      if (!missing(`owned`) && is.null(`owned`)) {
        stop("Invalid value for `owned` when calling ShoppingAdsApi$ListAds, `owned` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling ShoppingAdsApi$ListAds, `order` is not nullable")
      }

      if (!missing(`direction`) && is.null(`direction`)) {
        stop("Invalid value for `direction` when calling ShoppingAdsApi$ListAds, `direction` is not nullable")
      }

      if (!missing(`query`) && is.null(`query`)) {
        stop("Invalid value for `query` when calling ShoppingAdsApi$ListAds, `query` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling ShoppingAdsApi$ListAds, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling ShoppingAdsApi$ListAds, `collection_id` is not nullable")
      }
      if (!is.null(`collection_id`) && !stringr::str_detect(`collection_id`, "^\\d+(,\\d+)*$")) {
        stop("Invalid value for `collection_id` when calling ShoppingAdsApi$ListAds, must conform to the pattern ^\\d+(,\\d+)*$.")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling ShoppingAdsApi$ListAds, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling ShoppingAdsApi$ListAds, `language_code` is not nullable")
      }

      if (!missing(`prompt`) && is.null(`prompt`)) {
        stop("Invalid value for `prompt` when calling ShoppingAdsApi$ListAds, `prompt` is not nullable")
      }

      if (!missing(`prompt_type`) && is.null(`prompt_type`)) {
        stop("Invalid value for `prompt_type` when calling ShoppingAdsApi$ListAds, `prompt_type` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling ShoppingAdsApi$ListAds, `brand_kind` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling ShoppingAdsApi$ListAds, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling ShoppingAdsApi$ListAds, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling ShoppingAdsApi$ListAds, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling ShoppingAdsApi$ListAds, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`view`) && !(`view` %in% c("advertisers", "ads"))) {
        stop("Invalid value for view when calling ShoppingAdsApi$ListAds. Must be [advertisers, ads].")
      }
      query_params[["view"]] <- `view`

      query_params[["owned"]] <- `owned`

      if (!is.null(`order`) && !(`order` %in% c("ads", "prompts", "avg_position", "domain", "recent", "oldest", "position"))) {
        stop("Invalid value for order when calling ShoppingAdsApi$ListAds. Must be [ads, prompts, avg_position, domain, recent, oldest, position].")
      }
      query_params[["order"]] <- `order`

      if (!is.null(`direction`) && !(`direction` %in% c("asc", "desc"))) {
        stop("Invalid value for direction when calling ShoppingAdsApi$ListAds. Must be [asc, desc].")
      }
      query_params[["direction"]] <- `direction`

      query_params[["query"]] <- `query`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling ShoppingAdsApi$ListAds. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["prompt"]] <- `prompt`

      query_params[["prompt_type"]] <- `prompt_type`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling ShoppingAdsApi$ListAds. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling ShoppingAdsApi$ListAds. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/ads"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List shopping results
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per distinct product, or one per merchant (default value: "products")
    #' @param owned (optional) Return only products identified as the tracked brand's own. On view=merchants this narrows to the merchants selling those products; the totals block stays account-wide.
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param query (optional) Case-insensitive substring filter on the product title
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListShopping = function(project_id, page = 1, per_page = 20, view = "products", owned = NULL, order = NULL, direction = "desc", query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      local_var_response <- self$ListShoppingWithHttpInfo(project_id, page, per_page, view, owned, order, direction, query, model, collection_id, country_code, language_code, prompt, prompt_type, brand_kind, range, from, to, output, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List shopping results
    #'
    #' @param project_id Project ID
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param view (optional) Row shape: one per distinct product, or one per merchant (default value: "products")
    #' @param owned (optional) Return only products identified as the tracked brand's own. On view=merchants this narrows to the merchants selling those products; the totals block stays account-wide.
    #' @param order (optional) Sort field; the allowed set depends on view
    #' @param direction (optional) No description (default value: "desc")
    #' @param query (optional) Case-insensitive substring filter on the product title
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) One collection/tag ID or a comma-separated list of IDs. A query value is always a string on the wire, so it is typed as one: the previous integer-or-string union made generators emit a wrapper type they could not serialize.
    #' @param country_code (optional) One ISO country code or a comma-separated list (e.g. US,GB,DE)
    #' @param language_code (optional) One ISO language code or a comma-separated list (e.g. en,es,de)
    #' @param prompt (optional) Filter by prompt ID
    #' @param prompt_type (optional) One prompt type or a comma-separated list: informational, navigational, commercial, transactional
    #' @param brand_kind (optional) Filter by brand kind: brand (own brand/products), brand_other (competitors/other brands), non_brand (generic, no brand named). For fair 1:1 brand-vs-competitor comparisons (visibility, share of voice), use non_brand: brand-focused prompts skew results toward the brand they name. The in-app Overview page applies non_brand by default.
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListShoppingWithHttpInfo = function(project_id, page = 1, per_page = 20, view = "products", owned = NULL, order = NULL, direction = "desc", query = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, prompt = NULL, prompt_type = NULL, brand_kind = NULL, range = NULL, from = NULL, to = NULL, output = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling ShoppingAdsApi$ListShopping, `project_id` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling ShoppingAdsApi$ListShopping, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling ShoppingAdsApi$ListShopping, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListShopping, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListShopping, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling ShoppingAdsApi$ListShopping, must be bigger than or equal to 1.")
      }

      if (!missing(`view`) && is.null(`view`)) {
        stop("Invalid value for `view` when calling ShoppingAdsApi$ListShopping, `view` is not nullable")
      }

      if (!missing(`owned`) && is.null(`owned`)) {
        stop("Invalid value for `owned` when calling ShoppingAdsApi$ListShopping, `owned` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling ShoppingAdsApi$ListShopping, `order` is not nullable")
      }

      if (!missing(`direction`) && is.null(`direction`)) {
        stop("Invalid value for `direction` when calling ShoppingAdsApi$ListShopping, `direction` is not nullable")
      }

      if (!missing(`query`) && is.null(`query`)) {
        stop("Invalid value for `query` when calling ShoppingAdsApi$ListShopping, `query` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling ShoppingAdsApi$ListShopping, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling ShoppingAdsApi$ListShopping, `collection_id` is not nullable")
      }
      if (!is.null(`collection_id`) && !stringr::str_detect(`collection_id`, "^\\d+(,\\d+)*$")) {
        stop("Invalid value for `collection_id` when calling ShoppingAdsApi$ListShopping, must conform to the pattern ^\\d+(,\\d+)*$.")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling ShoppingAdsApi$ListShopping, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling ShoppingAdsApi$ListShopping, `language_code` is not nullable")
      }

      if (!missing(`prompt`) && is.null(`prompt`)) {
        stop("Invalid value for `prompt` when calling ShoppingAdsApi$ListShopping, `prompt` is not nullable")
      }

      if (!missing(`prompt_type`) && is.null(`prompt_type`)) {
        stop("Invalid value for `prompt_type` when calling ShoppingAdsApi$ListShopping, `prompt_type` is not nullable")
      }

      if (!missing(`brand_kind`) && is.null(`brand_kind`)) {
        stop("Invalid value for `brand_kind` when calling ShoppingAdsApi$ListShopping, `brand_kind` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling ShoppingAdsApi$ListShopping, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling ShoppingAdsApi$ListShopping, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling ShoppingAdsApi$ListShopping, `to` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling ShoppingAdsApi$ListShopping, `output` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`view`) && !(`view` %in% c("products", "merchants"))) {
        stop("Invalid value for view when calling ShoppingAdsApi$ListShopping. Must be [products, merchants].")
      }
      query_params[["view"]] <- `view`

      query_params[["owned"]] <- `owned`

      if (!is.null(`order`) && !(`order` %in% c("appearances", "price", "rating", "title", "products", "avg_price", "avg_rating", "merchant"))) {
        stop("Invalid value for order when calling ShoppingAdsApi$ListShopping. Must be [appearances, price, rating, title, products, avg_price, avg_rating, merchant].")
      }
      query_params[["order"]] <- `order`

      if (!is.null(`direction`) && !(`direction` %in% c("asc", "desc"))) {
        stop("Invalid value for direction when calling ShoppingAdsApi$ListShopping. Must be [asc, desc].")
      }
      query_params[["direction"]] <- `direction`

      query_params[["query"]] <- `query`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus", "naver_ai", "baidu_ai"))) {
        stop("Invalid value for model when calling ShoppingAdsApi$ListShopping. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus, naver_ai, baidu_ai].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["prompt"]] <- `prompt`

      query_params[["prompt_type"]] <- `prompt_type`

      if (!is.null(`brand_kind`) && !(`brand_kind` %in% c("brand", "brand_other", "non_brand"))) {
        stop("Invalid value for brand_kind when calling ShoppingAdsApi$ListShopping. Must be [brand, brand_other, non_brand].")
      }
      query_params[["brand_kind"]] <- `brand_kind`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling ShoppingAdsApi$ListShopping. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      local_var_url_path <- "/dimensions/shopping"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
