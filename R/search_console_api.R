#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.47.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title SearchConsole operations
#' @description SearchConsoleApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetSearchConsolePages  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_sort <- "impressions" # character |  (Optional)
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#' var_search_type <- "web" # character | Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (Optional)
#' var_filters <- "[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}]" # character | Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted. (Optional)
#' var_data_state <- "final" # character | final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (Optional)
#'
#' #Top Search Console pages (Growth+)
#' api_instance <- SearchConsoleApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetSearchConsolePages(var_project_id, range = var_range, from = var_from, to = var_to, sort = var_sort, page = var_page, per_page = var_per_page, output = var_output, search_type = var_search_type, filters = var_filters, data_state = var_data_state)
#'
#'
#' ####################  GetSearchConsoleQueries  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_sort <- "impressions" # character |  (Optional)
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#' var_search_type <- "web" # character | Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (Optional)
#' var_filters <- "[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}]" # character | Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted. (Optional)
#' var_data_state <- "final" # character | final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (Optional)
#'
#' #Top Search Console queries (Growth+)
#' api_instance <- SearchConsoleApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetSearchConsoleQueries(var_project_id, range = var_range, from = var_from, to = var_to, sort = var_sort, page = var_page, per_page = var_per_page, output = var_output, search_type = var_search_type, filters = var_filters, data_state = var_data_state)
#'
#'
#' ####################  GetSearchConsoleSummary  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_dimension <- "dimension_example" # character | Optional breakdown aggregated over the range. country and device are lowercased; page and query keep the casing Google returns, because a page URL is case sensitive. (Optional)
#' var_limit <- 56 # integer | Maximum breakdown rows, sorted by impressions descending. Default and maximum 1000. Use /search_console/queries or /search_console/pages to page through a full list. (Optional)
#' var_search_type <- "web" # character | Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (Optional)
#' var_filters <- "[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}]" # character | Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted. (Optional)
#' var_data_state <- "final" # character | final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (Optional)
#'
#' #Search Console summary (Growth+)
#' api_instance <- SearchConsoleApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetSearchConsoleSummary(var_project_id, range = var_range, from = var_from, to = var_to, dimension = var_dimension, limit = var_limit, search_type = var_search_type, filters = var_filters, data_state = var_data_state)
#'
#'
#' ####################  GetSearchConsoleTimeseries  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_range <- 56 # integer | Number of days to look back (alternative to from/to) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character | End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier. (Optional)
#' var_granularity <- "granularity_example" # character |  (Optional)
#' var_output <- "output_example" # character | Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON. (Optional)
#' var_search_type <- "web" # character | Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (Optional)
#' var_filters <- "[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}]" # character | Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted. (Optional)
#' var_data_state <- "final" # character | final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (Optional)
#'
#' #Search Console time series (Growth+)
#' api_instance <- SearchConsoleApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$GetSearchConsoleTimeseries(var_project_id, range = var_range, from = var_from, to = var_to, granularity = var_granularity, output = var_output, search_type = var_search_type, filters = var_filters, data_state = var_data_state)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SearchConsoleApi <- R6::R6Class(
  "SearchConsoleApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SearchConsoleApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Top Search Console pages (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param sort (optional) No description (default value: "impressions")
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetSearchConsolePages = function(project_id, range = NULL, from = NULL, to = NULL, sort = "impressions", page = 1, per_page = 20, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      local_var_response <- self$GetSearchConsolePagesWithHttpInfo(project_id, range, from, to, sort, page, per_page, output, search_type, filters, data_state, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Top Search Console pages (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param sort (optional) No description (default value: "impressions")
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetSearchConsolePagesWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, sort = "impressions", page = 1, per_page = 20, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling SearchConsoleApi$GetSearchConsolePages, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling SearchConsoleApi$GetSearchConsolePages, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling SearchConsoleApi$GetSearchConsolePages, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling SearchConsoleApi$GetSearchConsolePages, `to` is not nullable")
      }

      if (!missing(`sort`) && is.null(`sort`)) {
        stop("Invalid value for `sort` when calling SearchConsoleApi$GetSearchConsolePages, `sort` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling SearchConsoleApi$GetSearchConsolePages, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling SearchConsoleApi$GetSearchConsolePages, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsolePages, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsolePages, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsolePages, must be bigger than or equal to 1.")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling SearchConsoleApi$GetSearchConsolePages, `output` is not nullable")
      }

      if (!missing(`search_type`) && is.null(`search_type`)) {
        stop("Invalid value for `search_type` when calling SearchConsoleApi$GetSearchConsolePages, `search_type` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling SearchConsoleApi$GetSearchConsolePages, `filters` is not nullable")
      }

      if (!missing(`data_state`) && is.null(`data_state`)) {
        stop("Invalid value for `data_state` when calling SearchConsoleApi$GetSearchConsolePages, `data_state` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`sort`) && !(`sort` %in% c("impressions", "clicks", "ctr", "position"))) {
        stop("Invalid value for sort when calling SearchConsoleApi$GetSearchConsolePages. Must be [impressions, clicks, ctr, position].")
      }
      query_params[["sort"]] <- `sort`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling SearchConsoleApi$GetSearchConsolePages. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      if (!is.null(`search_type`) && !(`search_type` %in% c("web", "image", "video", "news", "discover", "googleNews"))) {
        stop("Invalid value for search_type when calling SearchConsoleApi$GetSearchConsolePages. Must be [web, image, video, news, discover, googleNews].")
      }
      query_params[["search_type"]] <- `search_type`

      query_params[["filters"]] <- `filters`

      if (!is.null(`data_state`) && !(`data_state` %in% c("final", "all"))) {
        stop("Invalid value for data_state when calling SearchConsoleApi$GetSearchConsolePages. Must be [final, all].")
      }
      query_params[["data_state"]] <- `data_state`

      local_var_url_path <- "/search_console/pages"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Top Search Console queries (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param sort (optional) No description (default value: "impressions")
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetSearchConsoleQueries = function(project_id, range = NULL, from = NULL, to = NULL, sort = "impressions", page = 1, per_page = 20, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      local_var_response <- self$GetSearchConsoleQueriesWithHttpInfo(project_id, range, from, to, sort, page, per_page, output, search_type, filters, data_state, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Top Search Console queries (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param sort (optional) No description (default value: "impressions")
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetSearchConsoleQueriesWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, sort = "impressions", page = 1, per_page = 20, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling SearchConsoleApi$GetSearchConsoleQueries, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling SearchConsoleApi$GetSearchConsoleQueries, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling SearchConsoleApi$GetSearchConsoleQueries, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling SearchConsoleApi$GetSearchConsoleQueries, `to` is not nullable")
      }

      if (!missing(`sort`) && is.null(`sort`)) {
        stop("Invalid value for `sort` when calling SearchConsoleApi$GetSearchConsoleQueries, `sort` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling SearchConsoleApi$GetSearchConsoleQueries, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling SearchConsoleApi$GetSearchConsoleQueries, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsoleQueries, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsoleQueries, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling SearchConsoleApi$GetSearchConsoleQueries, must be bigger than or equal to 1.")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling SearchConsoleApi$GetSearchConsoleQueries, `output` is not nullable")
      }

      if (!missing(`search_type`) && is.null(`search_type`)) {
        stop("Invalid value for `search_type` when calling SearchConsoleApi$GetSearchConsoleQueries, `search_type` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling SearchConsoleApi$GetSearchConsoleQueries, `filters` is not nullable")
      }

      if (!missing(`data_state`) && is.null(`data_state`)) {
        stop("Invalid value for `data_state` when calling SearchConsoleApi$GetSearchConsoleQueries, `data_state` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`sort`) && !(`sort` %in% c("impressions", "clicks", "ctr", "position"))) {
        stop("Invalid value for sort when calling SearchConsoleApi$GetSearchConsoleQueries. Must be [impressions, clicks, ctr, position].")
      }
      query_params[["sort"]] <- `sort`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling SearchConsoleApi$GetSearchConsoleQueries. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      if (!is.null(`search_type`) && !(`search_type` %in% c("web", "image", "video", "news", "discover", "googleNews"))) {
        stop("Invalid value for search_type when calling SearchConsoleApi$GetSearchConsoleQueries. Must be [web, image, video, news, discover, googleNews].")
      }
      query_params[["search_type"]] <- `search_type`

      query_params[["filters"]] <- `filters`

      if (!is.null(`data_state`) && !(`data_state` %in% c("final", "all"))) {
        stop("Invalid value for data_state when calling SearchConsoleApi$GetSearchConsoleQueries. Must be [final, all].")
      }
      query_params[["data_state"]] <- `data_state`

      local_var_url_path <- "/search_console/queries"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Console summary (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param dimension (optional) Optional breakdown aggregated over the range. country and device are lowercased; page and query keep the casing Google returns, because a page URL is case sensitive.
    #' @param limit (optional) Maximum breakdown rows, sorted by impressions descending. Default and maximum 1000. Use /search_console/queries or /search_console/pages to page through a full list.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetSearchConsoleSummary = function(project_id, range = NULL, from = NULL, to = NULL, dimension = NULL, limit = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      local_var_response <- self$GetSearchConsoleSummaryWithHttpInfo(project_id, range, from, to, dimension, limit, search_type, filters, data_state, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Console summary (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param dimension (optional) Optional breakdown aggregated over the range. country and device are lowercased; page and query keep the casing Google returns, because a page URL is case sensitive.
    #' @param limit (optional) Maximum breakdown rows, sorted by impressions descending. Default and maximum 1000. Use /search_console/queries or /search_console/pages to page through a full list.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetSearchConsoleSummaryWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, dimension = NULL, limit = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling SearchConsoleApi$GetSearchConsoleSummary, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling SearchConsoleApi$GetSearchConsoleSummary, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling SearchConsoleApi$GetSearchConsoleSummary, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling SearchConsoleApi$GetSearchConsoleSummary, `to` is not nullable")
      }

      if (!missing(`dimension`) && is.null(`dimension`)) {
        stop("Invalid value for `dimension` when calling SearchConsoleApi$GetSearchConsoleSummary, `dimension` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling SearchConsoleApi$GetSearchConsoleSummary, `limit` is not nullable")
      }
      if (!is.null(`limit`) && `limit` >  1000) {
        stop("Invalid value for `limit` when calling SearchConsoleApi$GetSearchConsoleSummary, must be smaller than or equal to 1000.")
      }
      if (!is.null(`limit`) && `limit` <  1) {
        stop("Invalid value for `limit` when calling SearchConsoleApi$GetSearchConsoleSummary, must be bigger than or equal to 1.")
      }

      if (!missing(`search_type`) && is.null(`search_type`)) {
        stop("Invalid value for `search_type` when calling SearchConsoleApi$GetSearchConsoleSummary, `search_type` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling SearchConsoleApi$GetSearchConsoleSummary, `filters` is not nullable")
      }

      if (!missing(`data_state`) && is.null(`data_state`)) {
        stop("Invalid value for `data_state` when calling SearchConsoleApi$GetSearchConsoleSummary, `data_state` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`dimension`) && !(`dimension` %in% c("country", "device", "page", "query", "searchAppearance"))) {
        stop("Invalid value for dimension when calling SearchConsoleApi$GetSearchConsoleSummary. Must be [country, device, page, query, searchAppearance].")
      }
      query_params[["dimension"]] <- `dimension`

      query_params[["limit"]] <- `limit`

      if (!is.null(`search_type`) && !(`search_type` %in% c("web", "image", "video", "news", "discover", "googleNews"))) {
        stop("Invalid value for search_type when calling SearchConsoleApi$GetSearchConsoleSummary. Must be [web, image, video, news, discover, googleNews].")
      }
      query_params[["search_type"]] <- `search_type`

      query_params[["filters"]] <- `filters`

      if (!is.null(`data_state`) && !(`data_state` %in% c("final", "all"))) {
        stop("Invalid value for data_state when calling SearchConsoleApi$GetSearchConsoleSummary. Must be [final, all].")
      }
      query_params[["data_state"]] <- `data_state`

      local_var_url_path <- "/search_console/summary"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Console time series (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param granularity (optional) No description
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetSearchConsoleTimeseries = function(project_id, range = NULL, from = NULL, to = NULL, granularity = NULL, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      local_var_response <- self$GetSearchConsoleTimeseriesWithHttpInfo(project_id, range, from, to, granularity, output, search_type, filters, data_state, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Console time series (Growth+)
    #'
    #' @param project_id Project ID
    #' @param range (optional) Number of days to look back (alternative to from/to)
    #' @param from (optional) No description
    #' @param to (optional) End of the window. A date-only value such as 2026-09-01 covers that whole day. Pass a full timestamp to end the window earlier.
    #' @param granularity (optional) No description
    #' @param output (optional) Rectangular output for BI tools (Tableau, Excel, Sheets, ELT). Omit for the default nested JSON. 'flat' returns the same metadata plus 'columns' and 'rows'; 'csv' returns those rows as text/csv. Errors are always returned as JSON.
    #' @param search_type (optional) Which search surface to measure. Defaults to web. discover and googleNews carry no query dimension, so Google rejects /search_console/queries for them. (default value: "web")
    #' @param filters (optional) Narrow the query; every entry must match (AND). Send the whole list as one JSON value: filters=[{\"dimension\":\"page\",\"operator\":\"contains\",\"expression\":\"/blog/\"}] (URL-encoded). An array of objects has no query-parameter form a generated client can produce, so the string is what the official SDKs send; see the SearchConsoleFilters schema for the shape it encodes. includingRegex and excludingRegex take RE2 syntax. At most 10 entries, expression at most 500 characters. The bracket form filters[][dimension]=page&filters[][operator]=contains&filters[][expression]=/blog/ is also accepted.
    #' @param data_state (optional) final (default) counts only rows Google has finalized. all also counts the most recent days, which are still being filled in and will change. (default value: "final")
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetSearchConsoleTimeseriesWithHttpInfo = function(project_id, range = NULL, from = NULL, to = NULL, granularity = NULL, output = NULL, search_type = "web", filters = NULL, data_state = "final", ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `project_id` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `range` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `to` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `granularity` is not nullable")
      }

      if (!missing(`output`) && is.null(`output`)) {
        stop("Invalid value for `output` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `output` is not nullable")
      }

      if (!missing(`search_type`) && is.null(`search_type`)) {
        stop("Invalid value for `search_type` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `search_type` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `filters` is not nullable")
      }

      if (!missing(`data_state`) && is.null(`data_state`)) {
        stop("Invalid value for `data_state` when calling SearchConsoleApi$GetSearchConsoleTimeseries, `data_state` is not nullable")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["range"]] <- `range`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      if (!is.null(`granularity`) && !(`granularity` %in% c("day", "week", "month"))) {
        stop("Invalid value for granularity when calling SearchConsoleApi$GetSearchConsoleTimeseries. Must be [day, week, month].")
      }
      query_params[["granularity"]] <- `granularity`

      if (!is.null(`output`) && !(`output` %in% c("flat", "csv"))) {
        stop("Invalid value for output when calling SearchConsoleApi$GetSearchConsoleTimeseries. Must be [flat, csv].")
      }
      query_params[["output"]] <- `output`

      if (!is.null(`search_type`) && !(`search_type` %in% c("web", "image", "video", "news", "discover", "googleNews"))) {
        stop("Invalid value for search_type when calling SearchConsoleApi$GetSearchConsoleTimeseries. Must be [web, image, video, news, discover, googleNews].")
      }
      query_params[["search_type"]] <- `search_type`

      query_params[["filters"]] <- `filters`

      if (!is.null(`data_state`) && !(`data_state` %in% c("final", "all"))) {
        stop("Invalid value for data_state when calling SearchConsoleApi$GetSearchConsoleTimeseries. Must be [final, all].")
      }
      query_params[["data_state"]] <- `data_state`

      local_var_url_path <- "/search_console/timeseries"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
