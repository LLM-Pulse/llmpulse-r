#' LLM Pulse API
#'
#' REST API client for LLM Pulse AI visibility analytics.
#'
#' The version of the OpenAPI document: 1.22.0
#' Contact: info@llmpulse.ai
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Sentiments operations
#' @description SentimentsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ListSentimentRecords  ####################
#'
#' library(llmpulse)
#' var_project_id <- 56 # integer | Project ID
#' var_competitor_id <- 56 # integer |  (Optional)
#' var_brand_only <- "brand_only_example" # character |  (Optional)
#' var_analysis <- "analysis_example" # character |  (Optional)
#' var_model <- "model_example" # character | Filter by AI model. Models the API key's user has not enabled are silently dropped. (Optional)
#' var_collection_id <- 56 # integer |  (Optional)
#' var_country_code <- "country_code_example" # character | ISO country code (e.g. US, GB, DE) (Optional)
#' var_language_code <- "language_code_example" # character | ISO language code (e.g. en, es, de) (Optional)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character |  (Optional)
#' var_page <- 1 # integer |  (Optional)
#' var_per_page <- 20 # integer |  (Optional)
#'
#' #List sentiment records
#' api_instance <- SentimentsApi$new()
#'
#' # Configure HTTP bearer authorization: BearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$ListSentimentRecords(var_project_id, competitor_id = var_competitor_id, brand_only = var_brand_only, analysis = var_analysis, model = var_model, collection_id = var_collection_id, country_code = var_country_code, language_code = var_language_code, from = var_from, to = var_to, page = var_page, per_page = var_per_page)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SentimentsApi <- R6::R6Class(
  "SentimentsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SentimentsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' List sentiment records
    #'
    #' @param project_id Project ID
    #' @param competitor_id (optional) No description
    #' @param brand_only (optional) No description
    #' @param analysis (optional) No description
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) No description
    #' @param country_code (optional) ISO country code (e.g. US, GB, DE)
    #' @param language_code (optional) ISO language code (e.g. en, es, de)
    #' @param from (optional) No description
    #' @param to (optional) No description
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return void
    ListSentimentRecords = function(project_id, competitor_id = NULL, brand_only = NULL, analysis = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, from = NULL, to = NULL, page = 1, per_page = 20, ...) {
      local_var_response <- self$ListSentimentRecordsWithHttpInfo(project_id, competitor_id, brand_only, analysis, model, collection_id, country_code, language_code, from, to, page, per_page, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List sentiment records
    #'
    #' @param project_id Project ID
    #' @param competitor_id (optional) No description
    #' @param brand_only (optional) No description
    #' @param analysis (optional) No description
    #' @param model (optional) Filter by AI model. Models the API key's user has not enabled are silently dropped.
    #' @param collection_id (optional) No description
    #' @param country_code (optional) ISO country code (e.g. US, GB, DE)
    #' @param language_code (optional) ISO language code (e.g. en, es, de)
    #' @param from (optional) No description
    #' @param to (optional) No description
    #' @param page (optional) No description (default value: 1)
    #' @param per_page (optional) No description (default value: 20)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ListSentimentRecordsWithHttpInfo = function(project_id, competitor_id = NULL, brand_only = NULL, analysis = NULL, model = NULL, collection_id = NULL, country_code = NULL, language_code = NULL, from = NULL, to = NULL, page = 1, per_page = 20, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (!missing(`project_id`) && is.null(`project_id`)) {
        stop("Invalid value for `project_id` when calling SentimentsApi$ListSentimentRecords, `project_id` is not nullable")
      }

      if (!missing(`competitor_id`) && is.null(`competitor_id`)) {
        stop("Invalid value for `competitor_id` when calling SentimentsApi$ListSentimentRecords, `competitor_id` is not nullable")
      }

      if (!missing(`brand_only`) && is.null(`brand_only`)) {
        stop("Invalid value for `brand_only` when calling SentimentsApi$ListSentimentRecords, `brand_only` is not nullable")
      }

      if (!missing(`analysis`) && is.null(`analysis`)) {
        stop("Invalid value for `analysis` when calling SentimentsApi$ListSentimentRecords, `analysis` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling SentimentsApi$ListSentimentRecords, `model` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling SentimentsApi$ListSentimentRecords, `collection_id` is not nullable")
      }

      if (!missing(`country_code`) && is.null(`country_code`)) {
        stop("Invalid value for `country_code` when calling SentimentsApi$ListSentimentRecords, `country_code` is not nullable")
      }

      if (!missing(`language_code`) && is.null(`language_code`)) {
        stop("Invalid value for `language_code` when calling SentimentsApi$ListSentimentRecords, `language_code` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling SentimentsApi$ListSentimentRecords, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling SentimentsApi$ListSentimentRecords, `to` is not nullable")
      }

      if (!missing(`page`) && is.null(`page`)) {
        stop("Invalid value for `page` when calling SentimentsApi$ListSentimentRecords, `page` is not nullable")
      }
      if (!is.null(`page`) && `page` <  1) {
        stop("Invalid value for `page` when calling SentimentsApi$ListSentimentRecords, must be bigger than or equal to 1.")
      }

      if (!missing(`per_page`) && is.null(`per_page`)) {
        stop("Invalid value for `per_page` when calling SentimentsApi$ListSentimentRecords, `per_page` is not nullable")
      }
      if (!is.null(`per_page`) && `per_page` >  100) {
        stop("Invalid value for `per_page` when calling SentimentsApi$ListSentimentRecords, must be smaller than or equal to 100.")
      }
      if (!is.null(`per_page`) && `per_page` <  1) {
        stop("Invalid value for `per_page` when calling SentimentsApi$ListSentimentRecords, must be bigger than or equal to 1.")
      }

      query_params[["project_id"]] <- `project_id`

      query_params[["competitor_id"]] <- `competitor_id`

      query_params[["brand_only"]] <- `brand_only`

      if (!is.null(`analysis`) && !(`analysis` %in% c("very_positive", "positive", "neutral", "negative", "very_negative"))) {
        stop("Invalid value for analysis when calling SentimentsApi$ListSentimentRecords. Must be [very_positive, positive, neutral, negative, very_negative].")
      }
      query_params[["analysis"]] <- `analysis`

      if (!is.null(`model`) && !(`model` %in% c("chatgpt", "perplexity", "gemini", "ai_overview", "ai_mode", "copilot", "claude", "grok", "deepseek", "meta_ai", "amazon_rufus"))) {
        stop("Invalid value for model when calling SentimentsApi$ListSentimentRecords. Must be [chatgpt, perplexity, gemini, ai_overview, ai_mode, copilot, claude, grok, deepseek, meta_ai, amazon_rufus].")
      }
      query_params[["model"]] <- `model`

      query_params[["collection_id"]] <- `collection_id`

      query_params[["country_code"]] <- `country_code`

      query_params[["language_code"]] <- `language_code`

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      query_params[["page"]] <- `page`

      query_params[["per_page"]] <- `per_page`

      local_var_url_path <- "/sentiments"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
